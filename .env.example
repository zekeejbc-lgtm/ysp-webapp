# Frontend feature flag: choose backend provider (gas | appwrite)
VITE_API_PROVIDER=gas

# Appwrite (frontend SDK)
# For the web app (no secrets here); obtain values from your Appwrite console
VITE_APPWRITE_ENDPOINT=
VITE_APPWRITE_PROJECT=
VITE_APPWRITE_DATABASE_ID=
VITE_APPWRITE_COL_USERS=
VITE_APPWRITE_COL_EVENTS=
VITE_APPWRITE_COL_ATTENDANCE=
VITE_APPWRITE_COL_ANNOUNCEMENTS=
VITE_APPWRITE_COL_FEEDBACK=
VITE_APPWRITE_COL_ACCESSLOGS=

# Optional: Push notifications (if configured)
# VITE_PUSH_PUBLIC_KEY=

# Migration script (server-side)
# These are used only by Node scripts and must NOT be exposed to the browser
# Copy this file to .env.local and fill in values before running migration
APPWRITE_ENDPOINT=
APPWRITE_PROJECT=
APPWRITE_API_KEY=
APPWRITE_DATABASE_ID=
APPWRITE_COL_USERS=
APPWRITE_COL_EVENTS=
APPWRITE_COL_ATTENDANCE=
APPWRITE_COL_ANNOUNCEMENTS=
APPWRITE_COL_FEEDBACK=
APPWRITE_COL_ACCESSLOGS=

# Google Sheets / Drive access for migration (choose ONE approach)
# 1) API Key (read-only public sheets) - limited
GOOGLE_SHEETS_API_KEY=

# 2) Service Account JSON (recommended for protected sheets)
# Store the JSON at a safe path and reference it here, or paste JSON directly as a single line
GOOGLE_SERVICE_ACCOUNT_JSON_PATH=
# or
GOOGLE_SERVICE_ACCOUNT_JSON=

# Source spreadsheet/document identifiers
GOOGLE_SHEETS_SPREADSHEET_ID=
GOOGLE_DRIVE_FOLDER_ID=

# Sheet tab names (headers at row 1, data starts at row 2)
# Override only if your spreadsheet uses different names
SHEET_TAB_USER_PROFILES=User Profiles
SHEET_TAB_MASTER_ATTENDANCE=Master Attendance Log
SHEET_TAB_ANNOUNCEMENTS=Announcements
SHEET_TAB_HOMEPAGE=Homepage Content
